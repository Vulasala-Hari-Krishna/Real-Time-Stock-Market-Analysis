# Hybrid Integration Handover

## Read This First

Last updated: **2026-09-26**. This is the model-independent progress ledger for
the Databricks/Snowflake migration. It must remain usable without the original
chat history, assistant memory, or access to a particular model.

- Branch: `feature/databricks_snowflake_impl`. Do not change branches or commit
  unless the user requests it. Preserve intervening user edits.
- Repository anchor before this documentation update: `07cb728`, with a clean
  worktree. Check `git status --short --branch` and `git log -5 --oneline` on resume;
  this anchor is historical, not a claim about future HEAD.
- **Current position: R1, R2, R3, and R4's core objectives are all DONE
  (2026-09-25/26).** The Databricks Free Edition platform bootstrap, the
  quote-processing job (Auto Loader -> bronze -> silver -> gold, on
  serverless compute), the gold snapshot exporter
  (`src/export/gold_snapshot.py`, reading via DuckDB's delta extension since
  `deltalake` cannot read Databricks' default deletionVectors-enabled
  tables), the Snowflake platform bootstrap (storage integration, warehouse,
  database, schema, non-admin loader role granted to a real user, `SERVING`
  schema, external stage over `publish/`), and the Snowflake snapshot loader
  (`src/load/snowflake_snapshot.py`) have all run live and succeeded, via
  six fully automated GitHub Actions workflows
  (`deploy-databricks-platform.yaml`, `deploy-databricks-job.yaml`,
  `export-gold-snapshot.yaml`, `deploy-snowflake-platform.yaml`,
  `load-snowflake-snapshot.yaml`, plus the AWS-only ones). A published batch
  (Parquet + manifest.json) exists in `publish/batches/` on S3, and it has
  now actually been loaded into `SERVING.DAILY_QUOTE_SUMMARY` in the
  Snowflake trial account (`ILMRWBU-TX52777`), with a `BATCH_LEDGER` row
  recorded. R4's first live run surfaced and fixed one real bug (a
  `bronze_version` lineage column missing from the loader's hand-written
  expected-schema registry); the second run succeeded end to end. Raw
  landing (local, opt-in) remains implemented but not yet exercised against
  live Kafka. See [snowflake/README.md](../snowflake/README.md).
- Prior AWS resources: the user confirmed a full AWS teardown/cleanup was
  completed before this session began, so the account started from a clean
  slate; the resources above are the first ones this migration has created.
- **R1-R5 are fully closed out and verified live.** R4: the Snowflake
  snapshot loader loaded a real batch, row-level content and idempotent
  replay both confirmed. R5: the dashboard's "Snowflake History" page,
  connecting as a new least-privilege `reader` role, showed real Snowflake
  data (not the demo fallback) in a live session. **R6 (historical/
  fundamental cloud migration) has its first product - fundamentals -
  implemented and unit tested (2026-09-26), following the user's explicit
  choice to migrate one legacy product at a time, starting with the
  simplest/most independent one.** New: `src/producers/fundamentals_fetcher.py`
  (local fetcher), `src/batch/landed_fundamentals.py` +
  `src/batch/databricks_fundamentals.py` (a second Databricks job, mirroring
  `databricks_ticks.py`'s bronze/silver/gold/state pattern), a broadened
  `landing` Unity Catalog external location (now the `landing` parent
  prefix, not just `landing/ticks`, so it covers `landing/fundamentals/`
  too), and a `fundamentals` dataset registered in both the R2 exporter and
  R4 loader (both were built generic across datasets specifically so this
  needed no redesign). **Nothing has been run live yet** - that's the next
  concrete action: re-apply the Databricks workspace Terraform root (for
  the broadened landing location), deploy/run the new
  `landed_fundamentals` job, run the fetcher, then export+load through the
  existing R2/R4 workflows with `dataset=fundamentals`.

### Reading Order

1. [Repository instructions](../.github/copilot-instructions.md) and the relevant
   [scoped instructions](../.github/instructions/).
2. This guide for progress, decisions, evidence, blockers, and remaining scope.
3. [Hybrid architecture and contracts](hybrid-migration.md) for storage, raw data,
   snapshot handoff, and local consumer operation.
4. [Databricks job guide](../databricks/README.md) for data semantics and retries.
5. [Platform bootstrap and destroy guide](../databricks/terraform/README.md) for
   account prerequisites, trust activation, Terraform state, and cleanup order.

The [main README](../README.md) begins with migration status; the older architecture,
diagrams, schedules, and examples below it describe the **legacy local system**.
This guide owns progress, the contract guide owns data contracts, and platform
guides own operational procedures. Update those owners instead of copying divergent
versions. Code/tests and measured runtime evidence take precedence over stale prose.

## Agreed Target

```text
LOCAL LAPTOP
  API -> producer -> Kafka -> raw consumer -> S3 landing/ticks/
                           |-> local live cache -> Streamlit (planned)
  Historical/fundamental fetchers --------> S3 landing/<dataset>/ (planned)

AWS + DATABRICKS
  S3 landing -> triggered Auto Loader -> bronze Delta -> silver Delta -> gold Delta
  Unity Catalog governs tables and S3 access; Jobs own internal dependencies
  Gold -> explicit immutable S3 exports -> completed manifest written last (planned)

SNOWFLAKE (planned)
  Completed exports -> COPY staging -> validation -> transactional native tables
  Native tables -> dimensional models / screening and reporting marts
  One X-Small warehouse provides compute; local Streamlit queries/caches results

LOCAL AIRFLOW (hybrid coordination planned)
  New-input gate -> Databricks run -> completed publication -> Snowflake -> reconcile
```

### Decisions To Preserve

- Personal project: minimize recurring billed activity. Kafka, producer, consumer,
  fetchers, Airflow/PostgreSQL, and Streamlit stay local. Preserve local Spark until
  its replacements are verified. No MSK, MWAA, EMR, or always-on cloud compute.
- S3 is the durable outbound-only boundary; do not expose Docker services to the
  internet or make Databricks connect to a laptop Kafka hostname.
- Raw landing files are not bronze tables. Databricks owns canonical bronze,
  silver, and gold Delta processing; do not create two competing silver pipelines.
- Consumer-landed events are the cloud stream source. Producer API backups stay
  separate; ingesting both as ticks would double-count source activity.
- Default cloud operation is manual, later optionally daily. No per-file compute
  startup, recurring tasks, or Snowpipe activation as incidental implementation.
- Snowflake owns replicated tables and SQL serving, not duplicate indicator logic.
  Its virtual warehouse is compute, not the physical storage layer.
- Start with small complete snapshots. CDC/CDF, Snowpipe, Streams/Tasks, declarative
  quality pipelines, and Dynamic Tables are later opt-in demonstrations.
- Full destructive cleanup of project data/resources is an explicit user requirement
  after use. Also support pause. Do not replace destroy with suspend-only behavior;
  do not execute destruction without explicit confirmation.
- Infrastructure ownership: CloudFormation for AWS; Terraform for Databricks and
  later Snowflake platform objects; bundles for Databricks jobs/code; versioned SQL
  for Snowflake models/migrations. Give every object/grant one owner.
- Never store secrets, live Terraform state, saved plans, or credentials in this
  document or Git. Collect authentication through local/CI secret mechanisms, not chat.
- **Databricks Free Edition is the target platform, not a paid workspace.**
  Verified 2026-09-24: Unity Catalog storage credentials/external locations
  against a self-owned S3 bucket work fully (Test Connection passed Read/List/
  Write/Delete/Path Exists/Assume Role/Self-Assume Role/External ID Condition),
  and a serverless notebook wrote/read a real Delta table through one. Free
  Edition has no classic compute at all (serverless only) and no account
  console/account-level API access; it does **not** lack external-S3 support,
  which earlier repository docs incorrectly assumed. The `databricks_cluster_policy`
  resource and its bundle `job_clusters` block were removed accordingly (see
  [workspace/main.tf](../databricks/terraform/workspace/main.tf) and
  [databricks.yml](../databricks/databricks.yml)); the job now runs on serverless
  compute via a bundle `environment_key`/`environments` block.
- **AWS region for this project's hybrid resources is `us-east-2`**, chosen to
  match the Databricks Free Edition workspace's fixed region (workspace region
  cannot be changed on Free Edition). CloudFormation stacks `01`/`05`/`06` and
  their GitHub Actions workflows default to `us-east-2` accordingly; the local/
  legacy stack defaults were also moved off `us-east-1` for consistency.
  Databricks workspace ID `7474656307742289`; region confirmed via a Unity
  Catalog metastore ID in the form `aws:us-east-2:<uuid>`.
- GitHub Actions with OIDC (`aws-actions/configure-aws-credentials`, role
  `secrets.AWS_DEPLOY_ROLE_ARN`) is the user's preferred deployment mechanism for
  every platform, not just AWS — avoid asking them to configure local AWS/
  Databricks/Snowflake CLI credentials. New workflows
  ([deploy-hybrid-infra.yaml](../.github/workflows/deploy-hybrid-infra.yaml),
  [teardown-hybrid-infra.yaml](../.github/workflows/teardown-hybrid-infra.yaml))
  extend this pattern to stacks `05`/`06`. A Databricks-Terraform-apply workflow
  is not yet built — it needs real stack `06` output values first, and the
  existing runbook's own "reviewed plan, not auto-approve" guidance argues for a
  GitHub Environment manual-approval gate rather than an unattended apply.

## Implementation Status

"Implemented/local" means code exists and recorded local checks passed; it does
not mean deployed, integrated, production-ready, or cost-measured.

| Workstream | Status | Primary evidence / remaining boundary |
|------------|--------|---------------------------------------|
| Architecture and coding guidance | Documented | Contract guide and scoped instructions; keep current |
| AWS storage foundation | Prepared/local | [S3 template](../cloudformation/01-s3-datalake.yaml); no live update verified |
| Raw capture | Implemented/local, opt-in | [Consumer](../src/consumers/raw_landing.py), [tests](../tests/unit/test_raw_landing.py); live Kafka/S3 pending |
| Databricks processing | Implemented/local | [Runner](../src/batch/databricks_ticks.py), [transforms](../src/batch/landed_ticks.py); Auto Loader/UC/Delta execution pending |
| Databricks platform IaC | Prepared/local | [Credential root](../databricks/terraform/credential/main.tf), [workspace root](../databricks/terraform/workspace/main.tf), [IAM role](../cloudformation/06-databricks-storage-role.yaml) |
| Manual job deployment | Prepared/local | [Bundle](../databricks/databricks.yml), [wheel configuration](../databricks/pyproject.toml); CLI/workspace validation pending |
| Gold export and manifest | Contract only | No exporter, executable manifest contract, or publication audit yet |
| Snowflake platform/load/marts | Platform live; loader implemented/local | Platform: [workspace root](../snowflake/terraform/workspace/main.tf), applied live. Loader: [src/load/snowflake_snapshot.py](../src/load/snowflake_snapshot.py), [SQL DDL](../snowflake/sql/), [tests](../tests/unit/test_snowflake_snapshot.py); no live load run yet - see [snowflake/README.md](../snowflake/README.md) |
| Historical/fundamental migration | R6 fundamentals slice implemented/local | [Fetcher](../src/producers/fundamentals_fetcher.py), [runner](../src/batch/databricks_fundamentals.py), [transforms](../src/batch/landed_fundamentals.py), [SQL DDL](../snowflake/sql/); registered in the R2 exporter and R4 loader. Not yet run live - existing local batch jobs remain unchanged; historical OHLCV/indicators still planned |
| Streamlit integration/live cache | R5 historical view DONE (live) | [Loader](../dashboards/snowflake_loader.py), [page](../dashboards/pages/snowflake_history.py), [tests](../tests/unit/test_snowflake_loader.py); read-only `reader` role in [workspace/main.tf](../snowflake/terraform/workspace/main.tf) applied and verified live with real query results; live cache (R7) still planned |
| Hybrid Airflow coordination | Planned | Existing local DAGs unchanged; no pre-compute new-input gate implemented |
| Deploy/pause/full destroy automation | Partial | Legacy AWS scripts exist; hybrid dependency order documented, not automated |
| CI | Implemented/local checks | [Workflow](../.github/workflows/ci.yaml) includes mocked Terraform checks; runs on main push/PR to main, not every feature-branch push |
| Cloud reconciliation/cost measurements | Not performed | Need authorized account access, run evidence, and billing observations |
| Advanced demonstrations | Deferred | No CDC/Snowpipe/Streams/Tasks/continuous pipelines to enable yet |

### What The Implemented Slices Actually Do

**Storage and access:** legacy bucket identities/paths are retained. The new
`landing/`, `lakehouse/`, `checkpoints/hybrid/`, and `publish/` boundaries avoid
expiry on active Delta files/checkpoints. Existing legacy `bronze/` still expires
after 30 days; the legacy gold cold-tier transition is removed in the template.
Multipart cleanup affects incomplete uploads only. Stack `05` creates unattached
policies; stack `06` attaches the Databricks policy to an initially unassumable role.

**Raw consumer:** disabled by default in [settings](../src/config/settings.py) and
the `hybrid-raw` [Compose profile](../docker/docker-compose.yaml). It retains source
bytes, tombstones, ordered headers, timestamps, and transport identity in gzip
NDJSON. SQLite stores pending records; S3 upload precedes explicit Kafka commits.
Restart/rebalance can replay records: delivery is at least once. Databricks must
deduplicate `(source_id, topic, partition, offset)`. Preserve `.state/` or the
`raw-landing-state` volume across normal stops, remove it deliberately during full
cleanup, and never replay old state after intentionally emptying the destination.

**Quote processing:** Auto Loader text mode retains raw lines in `ticks_raw`;
validation produces `quote_samples` and `ticks_quarantine`; gold is
`daily_quote_summary`; `ticks_pipeline_state` records processing/completion and
input/output versions. Conflicting transport/business identities are quarantined,
not arbitrarily overwritten. Tombstones are not implemented as historical deletes.
Gold is sampled quotes by `(provider, symbol, capture_date_utc)`, **not** exchange
OHLCV or the existing `daily_summaries` indicator product. Provider volume is kept
as last reported volume, never summed. Legacy polling time is not exchange time.

The first slice fully rebuilds up to a default 100,000 bronze rows. It pins source
versions, reconciles counts/keys, writes individual Delta snapshots, and marks
completion last. There is no cross-table Delta transaction. Consumers must require
completed state and read recorded versions. Increment `PIPELINE_REVISION` when
changing transformation semantics. The job has no schedule, queue, or automatic
retries; limits are 15 minutes for ingestion and 30 minutes overall. A manually
started no-op job still incurs startup cost until an external input gate is built.

**Platform bootstrap:** two independent Terraform roots avoid destructive mode
switching. Apply credential bootstrap only after the disabled role exists, use
generated principal/external ID to activate exact IAM trust, re-enable credential
validation, then create the workspace resources. Terraform creates policy metadata,
not a running cluster. Tables belong to the runtime; catalogs/schemas/locations
belong to Terraform. Managed catalog storage has its own nonoverlapping prefix.

## Next Session: Concrete Starting Point

**R1 is done (2026-09-25).** Stacks `01`-`06`, both Terraform roots, all 6
external locations, the catalog/schemas/service principal, and the actual
`landed_ticks` job all exist and ran successfully live in `us-east-2` against
the real project bucket and Databricks Free Edition workspace
(`7474656307742289`), via the fully automated
[deploy-databricks-platform.yaml](../.github/workflows/deploy-databricks-platform.yaml)
+ [deploy-databricks-job.yaml](../.github/workflows/deploy-databricks-job.yaml)
pipeline. Two real bugs were found and fixed along the way (IAM trust-policy
principal syntax; an empty-prefix Unity Catalog validation quirk) plus one
architecture correction (dropped `run_as: service_principal_name` — see the
2026-09-25 change-ledger rows for the full story before repeating either).

**Loose ends on R1, worth closing before calling it fully proven** (not
blocking R2, but cheap to do first):
1. Manually check `ticks_pipeline_state`'s `status` column and
   `quote_samples`/`ticks_quarantine` row counts in a Databricks notebook —
   the automated pipeline only proved the five tables exist and the job
   returned success, not their row-level contents (no SQL warehouse is
   deployed for this slice).
2. Re-run `deploy-databricks-job.yaml` a second time and confirm it's a
   correct no-op/skip (same bronze version, `snapshot_is_current` should
   return true) — replay/idempotency is one of R1's stated completion
   criteria and hasn't been exercised yet.
3. Land one fixture that's designed to fail validation (bad symbol, non-finite
   price, etc.) and confirm it lands in `ticks_quarantine` with a sensible
   `rejection_reason`, not silently dropped.

**Next real task: R2, the gold snapshot exporter and manifest.** This is the
piece that lets Snowflake (R3/R4) consume Databricks' gold output without
ever reading Delta's physical files directly. Concretely: read
`daily_quote_summary` at a pinned Delta version, write it as an immutable
Parquet snapshot to `publish/batches/<batch_id>/daily_quote_summary/`, and
write `publish/batches/<batch_id>/manifest.json` **last** (schema identity,
source table version, file list with checksums/row counts, `status:
completed`) — see the "Gold Snapshot Contract v1" section in
[docs/hybrid-migration.md](hybrid-migration.md#gold-snapshot-contract-v1) for
the full contract this must satisfy. No code exists for this yet — it needs
its own Python module, tests, and a decision on where it runs (a new
Databricks bundle task, or a local/Airflow-triggered job reading via the
Delta Python API).

To tear down the Databricks platform, run
[teardown-databricks-platform.yaml](../.github/workflows/teardown-databricks-platform.yaml)
(same catalog/schema_prefix/credential_name inputs as the deploy run) — it
deliberately does not delete stack `00`'s state bucket/lock table
(`DeletionPolicy: Retain`); remove those manually only after confirming both
Terraform roots destroyed cleanly.

## Remaining Roadmap And Completion Criteria

| ID | Work to implement or verify | Done when |
|----|----------------------------|-----------|
| R1 | ~~Databricks Free Edition bootstrap and live quote slice (`us-east-2`)~~ | **DONE and fully verified 2026-09-25.** Live: platform bootstrap + job succeeded on serverless compute against the real bucket/workspace; row-level content checked (accepted quotes in `quote_samples`, `ticks_pipeline_state.status='completed'`, `ticks_quarantine` empty); replay/idempotency confirmed (identical fixture path re-run produced zero new rows — `snapshot_is_current` correctly skipped recomputation). Still open, lower priority: a deliberate quarantine-triggering fixture and a failure/recovery drill (kill mid-run, confirm `processing` state, rerun recovers) — not blocking, can fold into R2 testing or come back to later |
| R2 | ~~Gold snapshot exporter and manifest validation~~ | **Core objective DONE 2026-09-25.** Live run of `export-gold-snapshot.yaml` succeeded against the real `daily_quote_summary` table: `verify_published_batch` passed on real S3 data (not mocks), confirming the DuckDB-based read fix and the `credential_chain` S3 secret both work against a real OIDC session. Data files + `manifest.json` confirmed present in `publish/batches/<batch_id>/`. Not yet exercised: incomplete-batch handling (data files present, manifest missing - should be ignored by a reader), a stale/repeated batch-ID re-run, and a second dataset beyond `daily_quote_summary` - lower priority, can fold into R4's loader testing |
| R3 | ~~Snowflake platform IaC and safe identity bootstrap~~ | **Core objective DONE 2026-09-26.** Live end-to-end: `bootstrap` root (storage integration) -> IAM trust activation -> `workspace` root (warehouse/database/schema/loader role/grants/external stage) all applied successfully against the real trial account (`ILMRWBU-TX52777`) and the real S3 bucket. Took 3 real attempts to get right (org/account provider config, the precondition-blocks-whole-apply architecture fix, the stage preview-feature/deprecation fix) - see the 2026-09-25/26 change-ledger rows for the full story. Not yet exercised: an actual load into the staging schema (that's R4), and no teardown workflow exists yet for this platform - worth building before the 30-day trial matters, but not blocking R4 |
| R4 | ~~Snowflake snapshot loader and analytical model~~ | **DONE and fully verified 2026-09-26.** Live: `COPY INTO` staging -> validated -> atomic `DELETE`+`INSERT` publish into `SERVING.DAILY_QUOTE_SUMMARY`, with a `completed` `BATCH_LEDGER` row; row-level content confirmed correct; replay of the same batch confirmed as a `skipped_already_loaded` no-op. One real bug found and fixed along the way (missing `bronze_version` lineage column in the loader's schema registry) |
| R5 | ~~One historical Streamlit view using Snowflake~~ | **DONE and fully verified live 2026-09-26.** `dashboards/snowflake_loader.py` + `dashboards/pages/snowflake_history.py`, a read-only `reader` Snowflake role (separate from the R4 loader role, applied live) added to `workspace/main.tf`, 5-minute cached queries, explicit `LoadStatus` banner (never masks a failed connection as demo success), old dashboard pages/paths untouched. Live: connecting as `reader` and querying `SERVING.DAILY_QUOTE_SUMMARY` succeeded, returning the real 2 rows with `st.success` shown, zero exceptions |
| R6 | Historical/fundamental cloud migration | **First product (fundamentals) implemented 2026-09-26, not yet run live** - see the change ledger. Local fetcher lands raw sources; Databricks owns validation and the latest-per-symbol gold projection; registered in the existing R2/R4 exporter/loader. Remaining legacy products (historical OHLCV, then daily indicators/signals) still to migrate one at a time, per this row's original scope |
| R7 | Local live-data cache | Durable bounded local serving path with explicit freshness and outage semantics; useful live UI without waking Snowflake continuously |
| R8 | Local Airflow cross-platform workflow | Manual-only initially; skip compute before startup on no input; durable run/batch IDs, reconnect instead of duplicate submission, finite retries/timeouts, manifest gates, and reconciliation |
| R9 | Hybrid deployment, pause and full destroy automation | Preview/confirmation, staged dependency ordering, no secret outputs, active-run cancellation, data/version/checkpoint removal when confirmed, state preserved until verified, residual-resource checks |
| R10 | Portfolio completion and operational evidence | New architecture diagram/README replace legacy claims only when true; repeatable small demo, measured latency/cost, recovery/RBAC evidence, supported runtime/dependency documentation |
| R11 | Optional advanced demonstrations | Separately approved CDC/CDF bootstrap/order/tombstone/retention recovery; Snowpipe + Streams/Tasks, quality expectations, or suitable Dynamic Tables; off by default and stoppable |

R1 is the current gate. R2-R5 provide the first two-platform serving slice. R6/R7
extend business coverage; R8/R9 complete operations, with cleanup for each new
resource required as it is introduced. R11 is not a prerequisite for the baseline.
MLflow, Cortex, Iceberg interop, and duplicate pipelines were not agreed baseline
requirements. Do not add them merely to increase the technology list.

## Verification Record

The 2026-09-23 rows below are **recorded results from that implementation
session**. The 2026-09-24 row is new, real evidence from this session, gathered
directly in the user's actual Databricks account; it is not a new run of the
2026-09-23 checks. Rerun relevant gates after edits; do not treat counts as
permanently fixed or evidence of live cloud CI.

| Check | Recorded result | Boundary |
|-------|-----------------|----------|
| Free Edition external S3 storage (2026-09-24) | Unity Catalog storage credential + external location created via workspace UI against personal bucket `s3://vulasala-dbcks-de-project/`; "Test Connection" passed Read/List/Write/Delete/Path Exists/Assume Role/Self-Assume Role/External ID Condition (all permissions confirmed); a serverless notebook then wrote and read back a real Delta table through the same external location | Real Databricks Free Edition workspace (`7474656307742289`, `us-east-2`) and real AWS account; against a **personal test bucket**, not yet the project's own CFN-managed bucket/prefixes or the actual Auto Loader job |
| Full unit suite | 368 passed; 87.34% overall coverage, >=80% gate | Local Windows Python 3.12.10; external services mocked |
| Real Spark transformations | Four tests passed with installed wheel in Linux Spark 3.5.3/Python 3.11 container | Actual DataFrame semantics, not Auto Loader/UC/S3/Delta runtime validation |
| Windows Spark run | Python worker crashed | Not a successful validation; Linux run provided behavior evidence |
| Raw-consumer image | Built; non-root Python 3.11 offline SDK/SQLite smoke passed | No broker-to-S3 connectivity verification |
| Databricks wheel | Built and imported/executed from temporary installed package | No workspace deployment or bundle CLI validation |
| Terraform | Both roots validated; six mock tests passed with network disabled, **before** the 2026-09-24 removal of `databricks_cluster_policy`/`databricks_permissions` from the `workspace` root and the matching test-file update | Terraform 1.9.8, provider 1.88.0; simulated apply only. Re-run `terraform fmt -check && terraform validate && terraform test` for both roots after the 2026-09-24 edit — not done this session; Terraform CLI was unavailable in this environment (native and Docker) |
| AWS templates | All six passed cfn-lint; five IAM contract tests passed | Syntax/contracts, not deployed trust or grants. Unchanged by the 2026-09-24 edits (05/06 templates were not modified) |
| Code checks | Black, Ruff, mypy passed | Existing repository gates; re-run after 2026-09-24 edits, not done this session |
| Databricks bundle test (2026-09-24) | `test_bundle_is_manual_bounded_and_uses_explicit_platform_inputs` updated and passing (34/34 in `test_databricks_ticks.py`) for the new serverless `environment_key`/`environments` structure | Validates the YAML shape only; does not prove `databricks bundle validate` accepts this schema against a live workspace |
| Live `workspace` Terraform root apply (2026-09-25) | Real run against the actual project bucket/workspace via `deploy-databricks-platform.yaml`: service principal, catalog, 3 schemas, 3 schema grants, catalog grant, and 5 of 6 external locations (bronze/silver/gold/checkpoints/managed) created successfully. The 6th (`landing`, the only `read_only=true` location) failed: Unity Catalog validates read-only locations by listing real objects rather than a write-probe, and a brand-new bucket has zero objects under `landing/ticks/` yet — an empty prefix produces a misleading "does not have LIST permissions... No such file or directory" error even though the IAM policy is correct. Fixed by adding an automated placeholder-object step (`landing/ticks/.keep`) to the `bootstrap` job, before the `workspace-objects` apply | First live evidence the `workspace` root and its IAM/UC wiring genuinely work end-to-end on Free Edition against the real project bucket, not just the personal test bucket. Remote S3 state correctly persisted the 19 successfully-created resources through the failure; the next run only needs to create the missing `landing` location plus any grants that hadn't started yet — not re-run from scratch. The placeholder-object fix itself is unverified against a live re-run as of this ledger update |

Useful local commands from the repository root (use the selected interpreter):

```text
python -m pytest tests/unit -q --cov=src --cov-report=term-missing --cov-fail-under=80
python -m ruff check src/ tests/
python -m black --check src/ tests/
python -m mypy src/ --ignore-missing-imports
cfn-lint cloudformation/*.yaml
docker compose -f docker/docker-compose.yaml --profile hybrid-raw config --quiet
python -m pip wheel --no-deps --wheel-dir databricks/dist ./databricks
```

For each Terraform root: `terraform init -backend=false -input=false
-lockfile=readonly`, then `terraform fmt -check`, `terraform validate`, and
`terraform test`. Provider initialization downloads tooling; the checked-in tests
use mock providers, not real platform applies. CI uses the pinned Terraform Docker
image; see the workflow for reproducible commands. Do not expose secrets through
resolved Compose output. Cloud integration tests require separate authorization.

Tooling observed on the last implementation machine: native Terraform and
Databricks CLI were not available; Terraform ran in Docker. If the installed
`cfn-lint` Windows launcher is absent, invoke its console entry point through
Python package metadata; `python -m cfnlint` did not work in that installation.
PowerShell 5.1 needs careful native-argument quoting. Recheck tool availability on
resume rather than assuming this environment is unchanged.

## Known Gaps And Safety Boundaries

- The legacy deploy script prints credential outputs. Legacy teardown empties
  versioned buckets and handles only stacks `04/03/02/01`; it misses `05/06` and
  all Databricks/Snowflake resources. Destructive intent is valid; coverage and
  error reporting need improvement before hybrid use. See R9 and the runbook.
- Runtime workspace service-principal removal can leave an account-level identity
  or binding. Inventory both scopes during final cleanup. Bundle destroy does
  not delete external tables/data, credentials, state, or the workspace itself.
- Terraform `trust_activation_confirmed` is an operator acknowledgement, not proof
  that IAM works. False entitlements do not override inherited grants. Serverless
  job/task quotas, the bundle's `environment_key`/`environments` schema, and
  wheel permissions are untested against a live workspace.
- Consumer immutability is enforced by application convention/checks, not S3 Object
  Lock. Replayed files can consume duplicate storage; deduplication is downstream.
- No executed cross-cloud transaction exists. Current gold completion state is not
  the future export manifest; validate that manifest and publication ledger explicitly.
- New raw/export retention and noncurrent-version cleanup need a policy based on
  recovery windows. Removing a lifecycle transition does not reclassify old objects.
- Local shutdown does not cancel remote jobs. Account budgets/warehouse monitors
  are not guaranteed hard caps on all charges. Stored data and Snowflake retention/
  Fail-safe may remain billable after compute stops or objects are dropped.
- No measured cloud costs, production reliability, or recovery of never-captured
  market events may be claimed from the local tests.

## Change Ledger

This is a milestone ledger, not a fabricated per-file commit history. Git is the
source for exact diffs. Dates/revisions below describe completed prior work.

| Date | Milestone | Result / revision anchor |
|------|-----------|--------------------------|
| 2026-09-22 to 2026-09-23 | Architecture/instructions, storage foundation, raw consumer | Standard Copilot entry point/scoped rules, contracts, optional IAM policies, durable opt-in consumer and tests; `8a0f233` is the recorded foundation/raw snapshot |
| 2026-09-23 | Databricks quote slice | Runner, normalization/deduplication, sampled summaries, wheel/bundle, real Spark tests; `0f5ff64` |
| 2026-09-23 | Databricks platform IaC | Fail-closed role, two Terraform roots/locks, mock tests, CI and staged cleanup guide; `07cb728` |
| 2026-09-24 | Cross-assistant handover | This ledger/roadmap and durable instruction to maintain it; documentation-only, based on the preceding Git anchor |
| 2026-09-25 | Fully automated Databricks platform bootstrap/teardown pipeline | User merged the feature branch to `main` and asked for zero-manual-step, remote-state-backed IaC ("no manual creation... like big companies do") instead of the local-Docker/copy-paste sequence from 2026-09-24. Added [cloudformation/00-terraform-state.yaml](../cloudformation/00-terraform-state.yaml) (S3 bucket + DynamoDB lock table, both `DeletionPolicy: Retain`, deliberately outside the data bucket's teardown blast radius). Added `backend "s3" {}` to both `databricks/terraform/credential/main.tf` and `workspace/main.tf` (backend values supplied via `-backend-config` at init time; `-backend=false` offline validation unaffected) — required because the `credential` root is applied twice and a disposable CI runner has no local disk continuity between those applies. Added [deploy-databricks-platform.yaml](../.github/workflows/deploy-databricks-platform.yaml) (5 chained jobs: bootstrap state+role -> credential bootstrap apply -> activate real IAM trust -> credential validate apply -> workspace-objects apply, each job's outputs feeding the next automatically) and its reverse, [teardown-databricks-platform.yaml](../.github/workflows/teardown-databricks-platform.yaml). Updated [databricks/terraform/README.md](../databricks/terraform/README.md) to describe the remote backend and point to the new workflow as the primary path, keeping the manual step-by-step description as reference/troubleshooting docs. Verified: `cfn-lint` passed on the new template and the full `cloudformation/*.yaml` set; both new workflow YAML files parse; **not verified**: an actual run of either new workflow (needs `DATABRICKS_HOST`/`DATABRICKS_TOKEN` secrets added first, and stack `01` deployed in `us-east-2`), and `terraform validate`/`terraform test` were not re-run against the backend-block change (Terraform CLI still unavailable in this environment) |
| 2026-09-24 | Free Edition preflight and serverless re-plan | User confirmed AWS teardown complete and provided Databricks account details (workspace `7474656307742289`, `us-east-2`). Live-tested Unity Catalog storage credential + external location against a personal S3 bucket in the user's actual Free Edition workspace: Test Connection passed all checks, and a serverless notebook wrote/read a real Delta table through it — corrected the prior (incorrect) assumption that Free Edition cannot use external S3 storage. Removed `databricks_cluster_policy`/`databricks_permissions` from `workspace/main.tf` and the classic `job_clusters` block from `databricks.yml` (replaced with a serverless `environment_key`/`environments` block); updated the matching Terraform test (`workspace/safety.tftest.hcl`) and Python test (`tests/unit/test_databricks_ticks.py`); updated `databricks/README.md`, `databricks/terraform/README.md`, `docs/hybrid-migration.md` to state verified Free Edition compatibility instead of the earlier uncertainty. Moved the project's AWS region default from `us-east-1` to `us-east-2` in `deploy-infra.yaml`/`teardown-infra.yaml`. Added `cloudformation/deploy-hybrid.sh` + `teardown-hybrid.sh` and `.github/workflows/deploy-hybrid-infra.yaml` + `teardown-hybrid-infra.yaml` to bring stacks `05`/`06` under the same GitHub-Actions-with-OIDC pattern already used for stacks `01`-`04`, since the user deploys everything via GitHub Actions rather than local CLI credentials. Uncommitted; `pytest tests/unit/test_databricks_ticks.py` (34 passed) and a YAML syntax check on all edited workflow/bundle files were run — `terraform fmt/validate/test` for the edited `workspace` root was **not** re-run (Terraform CLI unavailable in this environment) and remains a gate before the next real apply |
| 2026-09-25 | First live run of `deploy-databricks-platform.yaml`; R1 platform bootstrap complete; job-deploy automation added | Real run against the actual AWS account/workspace surfaced and fixed two genuine bugs, not environment misconfiguration: (1) `06-databricks-storage-role.yaml`'s deny-all bootstrap statement used `Principal: '*'`, which IAM trust policies reject (they require a typed principal like `{"AWS": "*"}`, unlike S3-style resource policies) — fixed, and `tests/unit/test_databricks_platform.py` updated to match; (2) the `landing` external location (the only `read_only=true` one) failed Unity Catalog's creation-time validation with a misleading "no LIST permission" error, because a brand-new bucket has zero objects under `landing/ticks/` and UC can't use its usual write-probe validation on a read-only path — fixed by adding an automated placeholder-object step (`landing/ticks/.keep` via `aws s3api put-object`, using a real temp file after `--body /dev/null` itself hit an unrelated AWS CLI/botocore quirk) to the `bootstrap` job. After both fixes, a full run succeeded: catalog `stock_market_dev`, 3 schemas, all 6 external locations, the runtime service principal, and all grants exist live in `us-east-2` against the real project bucket. Remote state correctly preserved the 19 resources created before the `landing` failure, so the fix-and-retry only created what was missing. Added [deploy-databricks-job.yaml](../.github/workflows/deploy-databricks-job.yaml): reads `bundle_variables` directly from the `workspace` root's remote state (no manual copying), generates+uploads a small fixture tick file, runs `databricks bundle validate/deploy/run -t dev`, then checks the five owned tables exist via `databricks tables get`. The fixture-generation logic was verified locally by importing the real `src/batch/landed_ticks.py::normalize_envelope` and confirming the generated envelope passes (`rejection_reason: None`). **Not yet run**: `deploy-databricks-job.yaml` itself — this is the next concrete action |
| 2026-09-25 | First live run of `deploy-databricks-job.yaml`; `run_as: service_principal_name` abandoned for this slice | `bundle validate` passed live for the first time (proves the serverless `environment_key`/`environments` syntax works against a real workspace). `bundle deploy` got as far as actually creating the job, then failed: `run_as: service_principal_name` requires the deploying identity to hold the "Service Principal User" role on that exact service principal (403 `PERMISSION_DENIED`); workspace-admin status does not grant it. **Two wrong fixes attempted before landing on the real one** — recorded so the next session doesn't repeat them: (1) an earlier documentation edit this same day had claimed Free Edition's lack of account-console access meant this grant could be skipped; live evidence proved that wrong. (2) The first fix attempt added a `databricks_permissions` resource with a `service_principal_id` argument; provider 1.88.0 rejects that argument name entirely ("Unsupported argument"). Investigation found the actual mechanism is `databricks_access_control_rule_set`, an **account-level** rule-set resource (`name = "accounts/<id>/servicePrincipals/<app_id>/ruleSets/default"`) — whether that account-level API path works from a workspace-scoped provider/token on Free Edition is unverified, and guessing a third time risked another failed cycle. **Final decision:** stop trying to run the job as the service principal. `databricks/databricks.yml` no longer sets `run_as`; the job now runs as the deploying admin (the default when `run_as` is omitted). The `runtime` service principal and its Unity Catalog grants in `workspace/main.tf` are unchanged and still created — just not wired up as the job's execution identity. Reverted the `deployer_user_name` variable and `runtime_service_principal_user` resource from `workspace/main.tf`/`safety.tftest.hcl` and the matching workflow inputs. Corrected `databricks/README.md` and `databricks/terraform/README.md` a second time, this time to the actually-verified state. Full unit suite re-run and passing (368 passed, 87% coverage); YAML syntax re-validated on all edited workflows. **Not yet run**: `deploy-databricks-platform.yaml` (to pick up the reverted `workspace/main.tf`) followed by `deploy-databricks-job.yaml` again — this is the next concrete action, and should be the one that finally completes R1 |
| 2026-09-25 | **R1 complete**: second live run of `deploy-databricks-job.yaml` failed differently, fixed, third run succeeded | Second run got past the `run_as` fix and actually executed on serverless compute for ~2 minutes before failing: `[NOT_SUPPORTED_WITH_SERVERLESS] PERSIST TABLE is not supported on serverless compute`, from `classified = classify_ticks(bronze).cache()` in `src/batch/databricks_ticks.py`. Root cause verified against Databricks' own docs before fixing (not guessed): serverless compute runs on Spark Connect against shared elastic infrastructure with no stable executor to pin a cached block to, so the entire RDD-level API (cache/persist/unpersist/checkpoint, not just cache specifically) is unsupported there. Fix: removed `.cache()`/`.unpersist()` entirely; `classified` is recomputed on each of its several downstream actions instead, which is cheap given this slice's bounded (<=100,000 row) scale. Updated the matching mocked unit test to assert cache/unpersist are *not* called. Confirmed no other `.cache()`/`.persist()` calls exist anywhere in `src/`. Full suite re-passed (368 passed). Third run (`Deploy Databricks Job #3`) completed successfully: platform bootstrap + job both green against the real bucket/workspace in `us-east-2`, on serverless compute, with no manual steps. **R1's core objective is met.** Not yet exercised: replay/idempotency on a second run, a deliberate quarantine-triggering fixture, row-level content verification of `quote_samples`/`ticks_pipeline_state` (no SQL warehouse deployed to query them yet) — see "Next Session" for these as optional R1 hardening before R2 |
| 2026-09-25 | Row-level check found 3 accepted quotes, not 1; made the CI fixture idempotent | Manual notebook check of `quote_samples` found 3 rows, not the expected 1. Root cause (verified by reasoning through the pipeline's own dedup rules, not assumed): every `deploy-databricks-job.yaml` run uploads its fixture **before** attempting deploy/run, so both earlier failed attempts (the `run_as` failure and the `.cache()` failure) had already uploaded their fixture to a `GITHUB_RUN_ID`-unique S3 key before failing downstream. Auto Loader correctly picked up all three leftover files once a run finally succeeded; since each had a different `source_id` and `quote_timestamp` (both derived from the run ID/wall-clock time), none were transport or business duplicates under `classify_ticks`' own rules, so all three were legitimately accepted, not a bug. Zero rows in `ticks_quarantine` and `status = 'completed'` in `ticks_pipeline_state` confirmed classification worked correctly on all three. Fixed by making the fixture fully deterministic: fixed `source_id` (`ci-fixture-v1`), fixed timestamp (`2026-01-01T00:00:00+00:00`), fixed S3 key (`landing/ticks/ci-fixture/fixture-v1.json.gz`) instead of one unique per run. This also means future re-runs exercise the no-op/replay path (Auto Loader tracks already-seen files by path in its checkpoint, so re-uploading the same path should mean nothing new to ingest) rather than accumulating one more accepted quote per trigger. Re-verified the fixed-value fixture still passes `normalize_envelope` (`rejection_reason: None`). **Not yet run**: a fourth `deploy-databricks-job.yaml` execution to confirm the no-op/replay behavior actually happens as expected. The 3 already-ingested rows are permanent in bronze/silver/gold for this catalog's lifetime (bronze is append-only/immutable by design; `rebuild_outputs` always full-rebuilds from bronze, never deletes bronze rows) — harmless test debris, not cleaned up, since deleting it would need an explicit destructive action the user hasn't requested |
| 2026-09-25 | **R1 fully closed out**: replay/idempotency confirmed live | Two more runs happened using the pre-fix workflow before the fixed version actually took effect (2 more `GITHUB_RUN_ID`-tied rows, same root cause as before, not a new bug), then one run with the fix produced the `ci-fixture-v1` row at the fixed path — 6 accepted quotes total, 0 quarantined. A follow-up run of `deploy-databricks-job.yaml` with no changes produced **zero new rows** (still exactly 6) — Auto Loader found nothing new at the already-seen fixed path, `snapshot_is_current` correctly skipped the rebuild. This is real, live evidence of R1's replay/idempotency completion criterion, not assumed. R1 roadmap row updated to DONE with both row-level content and replay verified. Remaining optional R1 hardening (deliberate quarantine fixture, failure/recovery drill) deferred, not blocking. **Moving to R2** (gold snapshot exporter and manifest) next |
| 2026-09-25 | R2 exporter module implemented (not yet deployed/run) | Added [src/export/gold_snapshot.py](../src/export/gold_snapshot.py): reads a gold Delta table via the lightweight `deltalake` package (no Spark/Databricks compute - same non-Spark pattern the Streamlit dashboard already uses for gold reads), enforces the Gold Snapshot Contract v1 (pinned Delta version; business-key uniqueness; data files written before `manifest.json`; zero-row snapshots rejected unless explicitly allowed; an executable `verify_published_batch` re-derives every file's checksum/size/prefix before the manifest is trusted). Registered `daily_quote_summary` as the only supported dataset for now (`DATASET_BUSINESS_KEYS`, `DATASET_CUTOFF_COLUMN`), extensible for later datasets without a redesign. Verified the `pyarrow` `group_by().aggregate([])` uniqueness-check technique actually works as intended with a real local check (not assumed) before relying on it. Added `deltalake==0.17.0` to `requirements.txt` and installed it locally to run mypy/tests against the real import. 12 new tests in `tests/unit/test_gold_snapshot.py` (config path construction, uniqueness pass/fail, checksum/size/prefix verification pass/fail, full `run_export` publish-order assertion via a paired put/get mock, explicit-empty vs rejected-empty snapshots, unregistered-dataset rejection). Full suite: 380 passed (was 368), 87% coverage; ruff/black/mypy clean on the new files. **Not yet done**: no GitHub Actions workflow to run this against the real bucket/catalog yet (unlike the Databricks pieces, this needs no Databricks CLI/Terraform - just AWS OIDC creds + `pip install -r requirements.txt` + `python -m src.export.gold_snapshot`), and it has never been run against the real `daily_quote_summary` table produced by R1 |
| 2026-09-25 | Gold export workflow added; S3 auth hardened before first live run | Added [export-gold-snapshot.yaml](../.github/workflows/export-gold-snapshot.yaml): checkout -> pip install -> AWS OIDC -> read stack 01's bucket export -> `python -m src.export.gold_snapshot`. Simpler than the Databricks workflows (no Databricks CLI/Terraform needed at all). Before wiring it up, hardened `read_gold_table` to build explicit deltalake `storage_options` from the standard `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`/`AWS_SESSION_TOKEN`/`AWS_REGION` env vars (mirroring `dashboards/data_loader.py`'s existing `_s3_storage_options()`) rather than relying on deltalake's Rust backend picking up an OIDC session's env vars implicitly - genuinely untested either way, but explicit is safer than assumed, especially for the `AWS_SESSION_TOKEN` that an OIDC-assumed role adds (long-lived-key setups wouldn't have exercised this path). Full suite re-passed (380 passed, 87% coverage); ruff/black/mypy clean. **Not yet run**: this workflow has never executed against the real bucket/table - that's the next concrete action, and the true test of whether the storage_options hardening was actually necessary |
| 2026-09-25 | First live `export-gold-snapshot.yaml` run found a real dependency-version bug; fixed | The storage_options hardening worked (no auth error) - S3/AWS side was fine. Failed instead at `to_pyarrow_table()`: `DeltaProtocolError: The table has set these reader features: {'deletionVectors'} but these are not yet supported by the deltalake reader`. Root cause: `requirements.txt` pinned `deltalake==0.17.0` (copied from `dashboards/requirements.txt`'s existing pin for consistency, which turned out to import an already-outdated limitation instead of a good practice). Databricks' current runtime enables the Delta "deletion vectors" reader/writer feature by default on new Unity Catalog tables, and old delta-rs/deltalake releases refuse to read any table with that protocol feature set, even read-only, even though this job's writes are full overwrites that never use deletion vectors themselves. Verified (not assumed) before fixing: upgraded to `deltalake==1.6.6` (current latest stable) locally, then smoke-tested the *exact* API surface `read_gold_table` uses - `DeltaTable(path, version=, storage_options=)`, `.version()`, `.to_pyarrow_table()` - against a real local Delta table, confirming no API break across the 0.17->1.6 jump before trusting it. `pyarrow` stayed at 15.0.2 (deltalake 1.6.6 depends on a separate `arro3-core` package internally, not a pyarrow bump). Updated `requirements.txt`'s pin; full suite re-passed (380 passed, 87% coverage). **Related finding, not fixed**: `dashboards/requirements.txt` has the same stale `deltalake==0.17.0` pin. The dashboard currently reads the *legacy* local `gold/` S3 prefix (different tables, written by local classic Spark, unlikely to have deletion vectors enabled), so this isn't a live bug today - but if the dashboard is ever pointed at this hybrid catalog's gold tables, it would hit the identical error, and its own fallback chain (Delta -> plain Parquet -> demo data) would likely swallow it silently into synthetic demo data rather than a loud failure. Worth fixing when R5 (Streamlit view using Snowflake/Databricks output) is implemented, not before. **Not yet re-run**: `export-gold-snapshot.yaml` with the fixed dependency - that's the next concrete action |
| 2026-09-25 | The `deltalake==1.6.6` upgrade did NOT fix the deletionVectors error; real fix uses DuckDB for row reads | Second live run failed with the *identical* `DeltaProtocolError`, even on the just-upgraded latest release - the previous fix was an unjustified leap: only the `DeltaTable()` constructor/`.version()` API surface had been verified against 1.6.6, never actual reading of a deletionVectors-enabled table (impossible to fabricate locally without Spark at the time). Verified properly this time: wrote a local Delta table with `configuration={"delta.enableDeletionVectors": "true"}` via `deltalake.write_deltalake` itself, confirmed `deltalake==1.6.6` still raises the identical `DeltaProtocolError` on `to_pyarrow_table()` against it (so this is a real, current gap in delta-rs's Python `to_pyarrow_dataset` path, not a version-lag issue), then confirmed `DeltaTable(...).version()` still resolves fine on the same table (metadata-only, never touches row data), then confirmed DuckDB's `delta` extension (built on delta-kernel-rs, a different/newer implementation) reads the same table's rows successfully, including pinning an exact version via `delta_scan(path, version => N)`. Rewrote `read_gold_table`: `deltalake.DeltaTable(...).version()` still resolves the exact version to read (unaffected by the bug), then a new `_read_delta_rows()` helper reads the actual rows via DuckDB (`INSTALL delta; LOAD delta; INSTALL httpfs; LOAD httpfs; CREATE SECRET (TYPE s3, PROVIDER credential_chain, REGION ...); SELECT * FROM delta_scan(path, version => N)`, materialized via `.arrow().read_all()`). Added `duckdb==1.5.5` to `requirements.txt`. Added a new *unmocked* regression test (`test_read_delta_rows_handles_deletion_vector_table`, using `tmp_path` and a real local deletion-vector table) specifically so this exact bug can't silently regress - the earlier mocked tests would never have caught it, which is exactly how the first (wrong) fix slipped through unnoticed. Full suite: 381 passed (was 380), 87% coverage; ruff/black/mypy clean. **S3 credential_chain path for DuckDB is still genuinely unverified** - confirmed working for local paths only; the GitHub Actions OIDC session's S3 access via DuckDB's `httpfs`/`credential_chain` secret has never been exercised against real S3. **Not yet re-run**: `export-gold-snapshot.yaml` with this fix - that's the next concrete action, and specifically the first real test of the DuckDB S3 path |
| 2026-09-25 | **R2 core objective DONE**: third live `export-gold-snapshot.yaml` run succeeded | DuckDB's `credential_chain` S3 secret correctly picked up the GitHub Actions OIDC session's temporary credentials - the one remaining unverified piece from the previous fix. Full run succeeded end-to-end: read the real `daily_quote_summary` table (deletionVectors and all), wrote the Parquet part file, `verify_published_batch` passed against real S3 objects (not mocks), wrote `manifest.json` last. User confirmed both the workflow succeeded and the files are visible in the S3 bucket under `publish/batches/<batch_id>/`. R2 roadmap row updated to DONE for its core objective. Remaining, lower-priority and not blocking: incomplete-batch handling, a stale/repeated batch-ID re-run, and a second dataset - deferred to fold into R4's loader testing rather than done in isolation now. **Moving to R3** (Snowflake platform IaC and identity bootstrap) next, pending user direction |
| 2026-09-25 | R3 IaC written (not yet applied); Snowflake account preflight done | User confirmed a 30-day Snowflake trial account: `ILMRWBU-TX52777`, AWS `ap-southeast-1`, user `VULASALAHARIKRISHNA`, `ACCOUNTADMIN`. Noted plainly: this trial is cross-region from the AWS resources (`us-east-2`) - accepted as negligible-cost/latency rather than redoing signup to region-match. Generated an RSA key pair for key-pair (JWT) authentication (Snowflake's recommended non-interactive auth) directly via this session's Bash tool (confirmed to be Git Bash with real `openssl` available, resolving the user's `cmd.exe` "openssl not recognized" error) rather than having the user hand-run commands in the wrong shell; public key registered on the Snowflake user via `ALTER USER ... SET RSA_PUBLIC_KEY`, private key stored as GitHub secrets (`SNOWFLAKE_ACCOUNT`, `SNOWFLAKE_USER`, `SNOWFLAKE_PRIVATE_KEY`) and never displayed in this session's transcript. Added `*.p8`/`*.pem`/`rsa_key*` to `.gitignore` before generating the files, and separately cleaned up an unrelated leftover `scratch_delta_dv_versions/` directory (debris from a crashed R2 verification script) plus a `scratch_*/` gitignore rule while at it.<br><br>Verified several Snowflake Terraform provider (`snowflakedb/snowflake` v2.21.0) schema details directly against the provider's own docs before writing HCL, after being burned twice on unverified provider schemas earlier this migration (Databricks `databricks_permissions.service_principal_id`, `deltalake` version claims): confirmed `snowflake_storage_integration` is deprecated in favor of `snowflake_storage_integration_aws`; confirmed `snowflake_warehouse`'s `resource_monitor` argument exists directly (one fetch wrongly claimed it required a separate `execute` resource - cross-checked and found that wrong); confirmed `snowflake_account_role`/`snowflake_grant_privileges_to_account_role` (renamed from the older `snowflake_role`/`snowflake_grant_privileges_to_role`) and their `on_account_object`/`on_schema` block shapes from real example HCL. **Two things remain genuinely unverified** (flagged inline in the code and prominently in `snowflake/terraform/README.md`): the storage integration's `describe_output[0].iam_user_arn`/`external_id` list-indexing syntax, and the `stage_usage` grant's `on_schema_object` block shape (inferred from the confirmed `on_account_object`/`on_schema` patterns, not confirmed directly for a stage object) - either may need one fix-iteration against a real apply, the same acceptable-risk pattern already used successfully for Databricks.<br><br>Built: [`cloudformation/07-snowflake-storage-role.yaml`](../cloudformation/07-snowflake-storage-role.yaml) (fail-closed IAM role for the storage integration, same two-stage trust pattern as stack `06`, already `cfn-lint`-clean); [`snowflake/terraform/main.tf`](../snowflake/terraform/main.tf) (one root, not two like Databricks - only the external stage needs to wait for trust, so it's the only resource gated by `trust_activation_confirmed`; storage integration, XSMALL warehouse with a resource-monitor credit cap, database, staging schema, and a non-`ACCOUNTADMIN` loader role with `USAGE`-only grants all apply in the first pass) plus [`safety.tftest.hcl`](../snowflake/terraform/safety.tftest.hcl) (mock-provider tests, unrun - Terraform CLI unavailable this session, same limitation as always); [`deploy-snowflake-platform.yaml`](../.github/workflows/deploy-snowflake-platform.yaml) (bootstrap -> apply #1, with `continue-on-error: true` on the intentionally-precondition-failing first apply, plus an explicit check that the storage integration's outputs are non-empty to distinguish "the stage was correctly blocked" from "something else broke" -> activate-trust -> apply #2). No teardown workflow yet - deliberately deferred until the first real apply confirms or corrects the two unverified schema points, to avoid fixing the same thing twice. **Not yet run**: this entire workflow - that's the next concrete action, and the real test of both unverified schema points |
| 2026-09-25 | First live `deploy-snowflake-platform.yaml` run found two real bugs (one config, one workflow robustness); both fixed | First apply failed immediately at provider configuration, before creating anything: `Error: 260000: account is empty`, with a directly-informative warning right above it - provider `snowflakedb/snowflake` v2.x does **not** read the `SNOWFLAKE_ACCOUNT` env var (it's silently ignored unless an opt-in `PROVIDER_CONFIGURATION_ACCOUNT_FALLBACK` experiment is enabled); it wants `organization_name`/`account_name` as separate fields instead, matching the `ILMRWBU-TX52777` identifier's two halves. This is a real breaking change from the older `Snowflake-Labs/snowflake` provider generation most tutorials still show, not a guess gone wrong. Fixed: added `organization_name`/`account_name` variables to `main.tf`'s provider block; the workflow splits the existing `SNOWFLAKE_ACCOUNT` secret into both at runtime (`${SNOWFLAKE_ACCOUNT%%-*}` / `${SNOWFLAKE_ACCOUNT#*-}`) rather than requiring a new secret. Confirmed live, before the failure: `snowflake_warehouse.resource_monitor` and the `on_account_object`/`on_schema` grant blocks all parsed/planned cleanly - one fewer thing to worry about for the next run.<br><br>**Second bug, a robustness gap in the workflow itself**: because the apply failed at provider config, *zero* resources were ever created, so `terraform output -raw iam_principal_arn` in the "Capture generated IAM trust values" step printed multi-line `Warning: No outputs found` text to stdout instead of erroring cleanly; that text got captured into the bash variable and written into `$GITHUB_OUTPUT`, corrupting its expected `key=value` line format and crashing the run at GitHub's own "Evaluate and set job outputs" step with `Invalid format`. The `if [ -z ... ]` empty-check I'd written to catch "something other than the gated stage failed" never got a chance to fire, because the corruption happened one step earlier than that check. Fixed by switching to `terraform output -json` piped through `jq` with `// ""` fallbacks (`jq -r '.iam_principal_arn.value // ""'`), which always yields a clean single-line string regardless of whether the key exists - this closes the gap for any future failure mode, not just this specific one. Applied the same fix to both `platform-bootstrap` and `platform-finish` jobs. YAML re-validated; `snowflake/terraform/README.md` and `safety.tftest.hcl` updated to match (the mock test's `variables{}` block now includes `organization_name`/`account_name`, still unrun - Terraform CLI unavailable this session). **Not yet re-run**: `deploy-snowflake-platform.yaml` with both fixes - that's the next concrete action, and now the real test of the two schema points that were never reached before (`describe_output[0]` indexing, the `on_schema_object` stage grant) |
| 2026-09-26 | Second live attempt found a real architectural design flaw: `lifecycle.precondition` blocks the entire plan/apply, not just the gated resource | With the org/account fix in place, the apply got much further: Terraform successfully planned all 9 resources - confirming `snowflake_warehouse.resource_monitor` and the `on_account_object`/`on_schema` grant blocks all parse correctly, real progress - but then reported "Terraform planned the following actions, but then encountered a problem" and created **zero** resources, failing on `snowflake_stage.publish`'s precondition. This revealed a wrong assumption baked into the whole single-root design: preconditions are evaluated during *planning*, before any resource operations begin, so a failing one aborts the entire apply - it does not behave like the Databricks `landing` external-location incident (a *runtime* API failure, where independent resources still got created and committed to state). Those are two different failure mechanisms; I had conflated them when designing this root. My own `if [ -z "${PRINCIPAL}" ]` safety check in the previous fix actually worked exactly as intended here - it correctly detected "nothing was created" and stopped cleanly, no corruption this time.<br><br>**Real fix, not a syntax patch**: split `snowflake/terraform/` into two roots, `bootstrap/` (storage integration only - nothing gated, always safe to apply) and `workspace/` (warehouse/database/schema/role/grants/stage - applied exactly once, only after trust is confirmed, so its precondition never actually blocks anything in practice). This is structurally identical to the Databricks `credential`/`workspace` split, which I now understand was implicitly protecting against this exact issue all along, for a reason I hadn't articulated until it broke here. Rewrote `deploy-snowflake-platform.yaml` accordingly: `bootstrap` (state+role, unchanged) -> `snowflake-bootstrap` (bootstrap root apply, now a normal clean apply, no `continue-on-error` hack needed) -> `activate-trust` (unchanged) -> `snowflake-workspace` (workspace root apply, `trust_activation_confirmed=true`, consuming the bootstrap root's `integration_name` output). Split `safety.tftest.hcl` into matching `bootstrap/safety.tftest.hcl` and `workspace/safety.tftest.hcl` (still unrun - Terraform CLI unavailable this session). Rewrote `snowflake/terraform/README.md` to document both real attempts and the corrected architecture. YAML re-validated. **Not yet run**: this restructured workflow - that's the next concrete action, and now the real test of both remaining unverified schema points (`describe_output[0]` indexing, `on_schema_object`), since attempt 2 never reached actual resource creation either |
| 2026-09-26 | Third live attempt: the two-root split worked, real progress persisted, one more resource-type fix needed | The `bootstrap` root, `activate-trust`, and most of `workspace` succeeded for real this time - concrete proof the two-root fix was correct: `describe_output[0]` indexing worked live (the pipeline reached and passed through `activate-trust`, which depends on those exact outputs), and Terraform actually created 8 of 10 `workspace` resources (resource monitor, warehouse, loader role, all 3 non-stage grants, database, schema) before failing - these are now real objects persisted in remote state, not just a plan. The `stage_usage` grant's `on_schema_object` block (previously flagged as unverified/inferred) planned correctly too: `object_type = "STAGE"`, `object_name` known-after-apply, no error. **Both previously-unverified schema points are now confirmed correct.**<br><br>The actual failure was new and specific: `Error: snowflake_stage_resource is currently a preview feature, and must be enabled by adding snowflake_stage_resource to preview_features_enabled`. `snowflake_stage` (the generic resource used in the original design) is deprecated *and*, as of this provider version, gated behind an explicit preview-feature opt-in - a stricter restriction than a mere deprecation warning. Fixed the same way the earlier `storage_integration` deprecation was handled: switched to `snowflake_stage_external_s3`, the stable AWS-specific replacement the deprecation warning itself named, confirming its schema against the provider's own docs first (same argument names - `name`/`database`/`schema`/`url`/`storage_integration`/`comment` - so this was a resource-type rename, not a redesign). Updated `workspace/main.tf` (the stage resource, the `stage_usage` grant's reference to it, and the `stage_name` output) and `workspace/safety.tftest.hcl` to match; confirmed no other `snowflake_stage.` references remain anywhere in `snowflake/`. Updated `snowflake/terraform/README.md`'s status section to record this as attempt 3, with both schema points now marked confirmed rather than unverified. **Not yet re-run**: since 8 resources already exist in state, the next run should only need to create the stage (now correctly typed) and its grant - this should be the run that finally completes R3's core objective |
| 2026-09-26 | **R3 core objective DONE**: fourth live run succeeded end to end | The stage-resource-type fix worked: `snowflake_stage_external_s3.publish` and its `stage_usage` grant were created against the already-persisted 8 resources from the previous attempt, with no further errors. Full chain now live: `bootstrap` root (storage integration, trusted by AWS IAM) -> `activate-trust` -> `workspace` root (`STOCK_MARKET_DEV` database, `PUBLISH_STAGING` schema, `STOCK_MARKET_DEV_WH` XSMALL warehouse under a 10-credit resource monitor, `STOCK_MARKET_DEV_LOADER` non-`ACCOUNTADMIN` role with `USAGE`-only grants, `STOCK_MARKET_DEV_PUBLISH_STAGE` external stage scoped to `publish/`). R3 roadmap row updated to DONE. It took three real, distinct bugs across four attempts to get here (provider account/org config; the precondition-blocks-whole-plan architecture flaw; the stage deprecation/preview-feature gate) - each one found live and fixed with a verified, not guessed, correction, consistent with how R1 and R2 were also proven. **Moving to R4** (Snowflake snapshot loader - `COPY INTO` staging from the R2-published manifest, atomic publish, business-key/checksum validation) next, pending user direction. No teardown workflow exists yet for the Snowflake platform - worth building soon given the 30-day trial, but not blocking R4 |
| 2026-09-26 | Pre-R4 gap found and fixed: `STOCK_MARKET_DEV_LOADER` role existed but was never granted to any user | While designing R4 (the loader script needs to authenticate *as* the least-privilege loader role, per the Snowflake scoped instructions' "not `ACCOUNTADMIN` for application sessions"), noticed `workspace/main.tf` creates `snowflake_account_role.loader` and grants it privileges on the warehouse/database/schema/stage, but never grants the *role itself* to any user - so nothing could actually assume it as-is; every session would still default to `ACCOUNTADMIN`. Verified the correct resource (`snowflake_grant_account_role`, args `role_name`/`user_name`) against the provider's own docs before writing HCL, not guessed. Added `variable "loader_grantee_user"` (validated as an existing-username-shaped string; this grants an *existing* identity - the trial's sole admin login - not a new one created here) and `resource "snowflake_grant_account_role" "loader_to_user"` to [workspace/main.tf](../snowflake/terraform/workspace/main.tf); added the matching variable default and an `assert` block to [workspace/safety.tftest.hcl](../snowflake/terraform/workspace/safety.tftest.hcl) (still unrun locally - Terraform CLI unavailable in this environment, same limitation as always); added `-var="loader_grantee_user=${{ secrets.SNOWFLAKE_USER }}"` to the `snowflake-workspace` job's apply step in [deploy-snowflake-platform.yaml](../.github/workflows/deploy-snowflake-platform.yaml) (reusing the existing `SNOWFLAKE_USER` secret - the trial's one login - rather than adding a new secret). YAML re-validated; full unit suite re-run and passing (381 passed, 86.90% coverage - unaffected, this change touches only Terraform/YAML, no Python). **Not yet run**: this updated workflow against the live account - the `workspace` root will need a `terraform apply` picking up the new resource (previous state has 10 of the 10 originally-planned resources already created; this adds an 11th). That's the next concrete action, then proceed into building R4's actual loader (SQL DDL, Python module, tests, workflow) |
| 2026-09-26 | **R4 implemented and unit tested** (not yet run live) | Built the Snowflake snapshot loader end to end. Added a `SERVING` schema (`snowflake_schema.serving`, default name `SERVING`) plus a `USAGE, CREATE TABLE` grant to the loader role in [workspace/main.tf](../snowflake/terraform/workspace/main.tf) and a matching assertion in `safety.tftest.hcl` - separate from `PUBLISH_STAGING` so dashboards/marts only ever depend on the one published table, never disposable staging objects; bundled with the still-unapplied `loader_grantee_user` change above (one future `terraform apply` picks up both). Wrote versioned, idempotent DDL under [snowflake/sql/](../snowflake/sql/) (`001_control_batch_ledger.sql` - the durable `BATCH_LEDGER` replay/idempotency table, since COPY load history alone is not a sufficient audit trail per the contract; `002_staging_daily_quote_summary.sql`; `003_serving_daily_quote_summary.sql`, schema derived directly from `src/batch/landed_ticks.py::summarize_quotes`' actual output columns, not guessed). Added [src/load/snowflake_snapshot.py](../src/load/snowflake_snapshot.py): finds the latest batch with a *completed* manifest (never an in-flight one); re-validates the manifest's columns/business-keys against a fixed per-dataset registry (rejects schema drift rather than silently loading a differently-shaped table); reuses `gold_snapshot.verify_published_batch` to re-derive checksums rather than duplicating that logic; consults `BATCH_LEDGER` to make an already-completed batch_id a no-op and reject a batch whose source version is not newer than what's already loaded (stale/duplicate rejection); `COPY INTO` staging from the exact manifest-listed files only, casting every column explicitly from the Parquet VARIANT (never trusts an inferred type, never points at the whole prefix); validates staged row count and business-key uniqueness in Snowflake itself (standard tables don't enforce either); publishes via one transaction (`DELETE` + `INSERT`, never `TRUNCATE`/`CREATE OR REPLACE` inside it, since both implicitly commit in Snowflake) that also marks the ledger row `completed`; a failure anywhere rolls back and marks the ledger row `failed`, leaving the previous serving snapshot untouched. Key-pair (JWT) auth via the same `SNOWFLAKE_PRIVATE_KEY`/`SNOWFLAKE_USER` secrets the Terraform provider already uses - verified the PEM-to-DER conversion (`_private_key_der`) against a real locally-generated RSA key (with and without a passphrase) before trusting it, the same "verify against a real key/table before relying on it" discipline that caught the deletionVectors bug in R2. Added `snowflake-connector-python==3.13.2` and `cryptography==42.0.5` to `requirements.txt`, installed and imported locally to confirm no import/API surprises. Added [load-snowflake-snapshot.yaml](../.github/workflows/load-snowflake-snapshot.yaml): reads the already-applied `workspace` root's Terraform state for warehouse/database/schema/role/stage names (same no-manual-copying pattern as `deploy-databricks-job.yaml` reading `bundle_variables`) rather than re-typing values that could drift from what was actually deployed. Added 21 new tests in [tests/unit/test_snowflake_snapshot.py](../tests/unit/test_snowflake_snapshot.py) using a hand-written fake DB-API cursor/connection (records exact executed SQL/params, so the COPY INTO/transaction-building logic is asserted directly, not just mocked away) plus mocked S3/orchestration boundaries; covers schema-drift rejection, stale-version rejection, idempotent-replay skip, commit-vs-rollback behavior, and the full happy-path call order. Full suite: 402 passed (was 381), 85.20% coverage (loader module itself 73%, consistent with other partially-covered batch modules in this repo); ruff/black/mypy clean on all new files. YAML syntax validated for the new workflow. **Not yet run**: `terraform fmt/validate/test` for the `workspace` root (Terraform CLI still unavailable in this environment - same limitation as every prior Snowflake root), a live `workspace` apply to actually create the `SERVING` schema/grant, and the first live run of `load-snowflake-snapshot.yaml` itself - that's the next concrete action, and the true test of the COPY INTO column-cast syntax, the `on_schema_object`-style stage reference (`@{database}.{schema}.{stage}`), and the MERGE-based ledger upsert, none of which have been exercised against a real Snowflake account yet |
| 2026-09-26 | First live run of `load-snowflake-snapshot.yaml` found a real bug: `bronze_version` lineage column missing from the loader's schema registry | Good news first: `Deploy Snowflake Platform` had already been re-run successfully and picked up both pending changes (the `loader_grantee_user` grant and the new `SERVING` schema/grant) - confirmed by this run's own `terraform init`/`terraform output` against `workspace.tfstate` succeeding and returning real values for all six outputs (`database`, `staging_schema`, `serving_schema=SERVING`, `warehouse`, `role`, `stage`), and AWS/S3 auth, `pip install`, and `find_latest_batch_id`/`load_manifest` all worked correctly against the real bucket. The loader failed at `validate_expected_schema`: `ValueError: Schema drift for daily_quote_summary: expected columns [...12 columns...], manifest has [...13 columns, including 'bronze_version'...]`. Root cause: `DATASET_COLUMNS` in `src/load/snowflake_snapshot.py` was hand-derived only from `src/batch/landed_ticks.py::summarize_quotes`'s output columns; it missed `bronze_version`, a lineage column `databricks_ticks.py::rebuild_outputs` appends via `frame.withColumn("bronze_version", F.lit(bronze_version))` **after** `summarize_quotes()` runs, before writing the gold table - a real, pre-existing gap in the gold table's schema that this hand-fixed registry simply hadn't accounted for (the exporter's manifest reflects the table's actual columns dynamically, so it was correct all along; only the loader's separate fixed expectation was wrong). Fixed by adding `("bronze_version", "NUMBER(38,0)")` to `DATASET_COLUMNS` and a matching `bronze_version NUMBER(38, 0)` column to both `snowflake/sql/002_staging_daily_quote_summary.sql` and `003_serving_daily_quote_summary.sql` (neither table had been created yet - the failure happened before `ensure_objects_exist`/any Snowflake connection, so no `ALTER TABLE`/migration was needed, just correcting the DDL before first creation). Added a dedicated regression test (`test_validate_expected_schema_includes_bronze_version_lineage_column`) so this column can't silently drop from the registry again - the same "add a test that would have caught the actual bug" discipline used for R2's deletionVectors incident. Full suite: 403 passed (was 402), 85.20% coverage; ruff/black/mypy clean. **Not yet re-run**: `load-snowflake-snapshot.yaml` with this fix - that's the next concrete action, and the real test of everything past schema validation (COPY INTO's column casts, the `@database.schema.stage` reference, staging validation, the atomic publish transaction, and the ledger MERGE), none of which have been reached live yet |
| 2026-09-26 | **R4 core objective DONE**: second live run of `load-snowflake-snapshot.yaml` succeeded end to end (1m23s) | With the `bronze_version` fix in place, the run got past schema validation and completed successfully - the first time `COPY INTO`'s explicit column casts, the `@database.schema.stage` reference syntax, staging row-count/business-key validation, the atomic `DELETE`+`INSERT` publish transaction, and the `BATCH_LEDGER` MERGE upsert have all executed against the real account, with no further bugs surfacing. `ensure_objects_exist` also created all three tables (`BATCH_LEDGER`, `DAILY_QUOTE_SUMMARY_STAGING`, `DAILY_QUOTE_SUMMARY`) for the first time via the versioned SQL in `snowflake/sql/`. R4 roadmap row updated to DONE for its core objective. Row-level check confirmed (2026-09-26): `BATCH_LEDGER` shows batch `20260925T172452Z-157c9bf6`, `source_version=3`, `status='completed'`, `loaded_row_count=2` matching `manifest_row_count=2`; `SERVING.DAILY_QUOTE_SUMMARY` has exactly those 2 rows (both `ci_fixture`/`AAPL`, one per `capture_date_utc`), correctly stamped with `bronze_version=5`, the matching `loaded_batch_id`, and a real `loaded_at`. |
| 2026-09-26 | **R4 fully closed out**: replay/idempotency confirmed live | A follow-up run of `load-snowflake-snapshot.yaml` with no new batch published completed in seconds and logged `Batch 20260925T172452Z-157c9bf6 already loaded for daily_quote_summary; no-op` / `{'status': 'skipped_already_loaded', 'rows_loaded': 0}` - no new writes to `SERVING`/`BATCH_LEDGER`, confirming the ledger-based idempotent-replay guarantee works against the real account, not just in mocked tests. This is the same replay completion criterion R1 was held to. R4 roadmap row updated to DONE - full contract now proven live: exact manifest files loaded to staging, schema/row-count/business-key checks, atomic DML publish, ledger-based stale/duplicate rejection, and idempotent replay. No teardown workflow exists yet for either platform - still deliberately deferred per the user's earlier instruction. **Moving to R5** (one historical Streamlit view reading from Snowflake) next |
| 2026-09-26 | R5 implemented (not yet run live) | Built the Streamlit historical view. Verified the exact `on_schema_object` `all`/`future` block schema (`object_type_plural` + `in_schema`) against the provider's own docs before writing HCL, not guessed - same discipline as every prior Snowflake Terraform change this migration. Added to [workspace/main.tf](../snowflake/terraform/workspace/main.tf): a second `snowflake_account_role.reader` (deliberately separate from the R4 loader role - per the Snapshot Load Contract's "role-separated loader, transformer, and dashboard access" requirement), granted to the same trial user via `snowflake_grant_account_role`, with `USAGE` on the warehouse/database/`SERVING` schema and `SELECT` on all-existing-and-future tables in `SERVING` only (no staging/stage access at all). Matching assertions added to `safety.tftest.hcl` (still unrun - Terraform CLI unavailable, same as every prior root). Added [dashboards/snowflake_loader.py](../dashboards/snowflake_loader.py): connects read-only as the `reader` role via the same key-pair (JWT) credentials R4 uses; `@st.cache_data(ttl=300)` avoids re-opening a warehouse session on every page rerun; every call returns an explicit `LoadStatus(source, ok, message, as_of, batch_id)` alongside the DataFrame rather than silently substituting demo data - on any connection/query failure it still returns deterministic demo data (matching the rest of `dashboards/`'s local-dev convenience), but always paired with `ok=False`/`source="demo"` so the caller cannot mistake it for a real result. Added [dashboards/pages/snowflake_history.py](../dashboards/pages/snowflake_history.py) (new "Snowflake History" nav entry in `app.py`): renders that status as a prominent `st.success`/`st.error` banner - per the R5 completion criterion, a failed cloud connection must never masquerade as demo success, unlike the existing pages' quiet caption-only fallback. Added `snowflake-connector-python`/`cryptography` to `dashboards/requirements.txt`. Added 5 tests in [tests/unit/test_snowflake_loader.py](../tests/unit/test_snowflake_loader.py) (real key round-trip, deterministic demo data, success/failure/connection-cleanup paths), plus a headless smoke test via Streamlit's `AppTest` (not a permanent repo test, run manually this session): with no `SNOWFLAKE_PRIVATE_KEY` set, the full page renders with zero exceptions and correctly shows the `DEMO DATA` banner - proving the failure path is safe end-to-end, not just unit-mocked. Full suite: 408 passed (was 403); ruff clean on `dashboards/`; black applied only to the new test file (`dashboards/` itself is outside this repo's black/mypy gate - `make format`/`make lint` only target `src/`/`tests/`, and existing dashboard files were never black-formatted either, so reformatting them now would be an unrelated, out-of-scope diff). **Not yet run**: `terraform fmt/validate/test` for the `workspace` root, a live apply to actually create the `reader` role/grants, and pointing a real dashboard session at the live account - that's the next concrete action, and the true test of the `SELECT`-on-all/future-tables grant and the read-only role's session credentials |
| 2026-09-26 | Local `.env` setup for R5 found two real bugs: `Settings` rejected unrelated keys, and a naive escape script silently failed | While setting up the dashboard's local Snowflake credentials, the user added `SNOWFLAKE_ACCOUNT`/`SNOWFLAKE_USER`/`SNOWFLAKE_PRIVATE_KEY` to the shared repo-root `.env`. Two real, unrelated bugs surfaced from this single change. **(1)** The full unit suite started failing (`tests/unit/test_stock_producer.py`'s fixtures, which construct `Settings()`): `src/config/settings.py`'s `Settings` model has no `extra` config, so pydantic-settings' default `extra="forbid"` rejected the whole object over keys it doesn't declare, even though `.env` is intentionally shared with consumers (like the dashboard) that read `os.environ` directly rather than through `Settings`. Fixed by adding `"extra": "ignore"` to `Settings.model_config` - this model should validate its own known fields, not reject the file over keys it was never meant to own. **(2)** Converting the multi-line PEM key into the single-line, `\n`-escaped format documented in `snowflake/README.md` (needed because Docker Compose's `env_file` cannot reliably hold a real multi-line value) was first attempted via an inline `python -c "..."` command through nested Bash-tool/shell quoting; the escape replacement silently no-opped due to the quoting layers, so the real multi-line key got written into `.env` verbatim, splitting one env var across ~30 lines and corrupting the file structure (confirmed by line-count/structural inspection, without ever printing the actual key material into this session). Fixed by writing the conversion as a standalone script file instead of an inline shell one-liner (avoids the quoting fragility entirely), verified via the actual `_private_key_der` function successfully parsing the corrected `.env` value into a working key - not just visual inspection. **Defensive code fix, not just a one-off `.env` repair**: added `.replace("\\n", "\n")` un-escaping to both `src/load/snowflake_snapshot.py::_private_key_der` and `dashboards/snowflake_loader.py::_private_key_der`, so either module accepts a key with real newlines (CI/GitHub Actions secrets) or literal `\n` escapes (a local single-line `.env` value) without the caller needing to know which format is in play; added a regression test for the escaped-newline case to both test files. Full suite: 410 passed (was 408), 85.20% coverage. `.env` confirmed still gitignored (`git check-ignore -v .env` matches `.gitignore:34`) - never at risk of being committed |
| 2026-09-26 | `reader` role platform apply succeeded live (`Deploy Snowflake Platform #6`) | The `workspace` root re-apply picked up the new `reader` account role, its grant to the trial user, and the `USAGE`/`SELECT`-on-all-and-future-tables grants on `SERVING` - all created successfully with no errors, confirming the `on_schema_object` `all`/`future` block schema (verified against docs, not guessed) was correct on the first live attempt |
| 2026-09-26 | **R5 fully closed out**: live dashboard session confirmed real Snowflake data | With `.env` correctly holding `SNOWFLAKE_ACCOUNT`/`SNOWFLAKE_USER`/`SNOWFLAKE_PRIVATE_KEY`, ran the actual dashboard app (via Streamlit's `AppTest` harness loading real `.env` credentials, plus the container build via `docker compose up --build streamlit` for a real-browser check) with "Snowflake History" selected: connected as the `reader` role and queried `SERVING.DAILY_QUOTE_SUMMARY` successfully, rendering `st.success("Live from Snowflake — 2 rows, last loaded 2026-09-26 05:08 UTC (batch 20260925T172452Z-157c9bf6)")` with zero exceptions and the dataframe/chart rendering - the exact 2 rows verified earlier for R4. This is real evidence, not the demo-fallback path: `os.environ["SNOWFLAKE_PRIVATE_KEY"]` resolved, the connection succeeded, and the query returned real rows. R5 roadmap row updated to DONE |
| 2026-09-26 | **R6 started**: first legacy product (fundamentals) migrated, implemented and unit tested (not yet run live) | User explicitly chose to migrate fundamentals first (over historical OHLCV or daily indicators) - simplest, no historical-backfill design needed, no dependency on tick data. Investigated the real gold table schema before building (checked `databricks_ticks.py::rebuild_outputs` for the `bronze_version` lineage-column precedent, `src/common/schemas.py::FundamentalData` for the already-existing canonical model, and the CloudFormation IAM policies/Unity Catalog external locations for `landing/` prefix scoping) rather than guessing the design. Found one real pre-existing scoping gap: the Unity Catalog `landing` external location (`databricks/terraform/workspace/main.tf`) was hard-scoped to `landing/ticks`, not the `landing` parent - broadened it (one-line `path` change, `read_only=true`/`READ_FILES`-only so this only widens what Databricks can *read*) so `landing/fundamentals/` (and any future `landing/<dataset>/`) doesn't need its own external location; updated the matching `safety.tftest.hcl` assertion. Confirmed via the CloudFormation policies (`05-hybrid-access.yaml`) that no CFN change was needed - both landing IAM policies already use a generic `landing/*` resource pattern. <br><br>Added: [src/producers/fundamentals_fetcher.py](../src/producers/fundamentals_fetcher.py) (standalone, manually/periodically run - not a continuous poller like `stock_producer.py`, since fundamentals change slowly; fetches the whole watchlist via yfinance, validates each record through the existing `FundamentalData` model before upload, uploads one immutable content-addressed NDJSON batch per run, reusing `raw_landing.py`'s proven content-addressed-conflict-check upload pattern; never blocks on one bad symbol, but refuses to upload an empty batch if every symbol failed). [src/batch/landed_fundamentals.py](../src/batch/landed_fundamentals.py) + [src/batch/databricks_fundamentals.py](../src/batch/databricks_fundamentals.py) (a second, independent Databricks job in the same bundle, structurally mirroring `databricks_ticks.py`'s bronze/silver/gold/state/serverless pattern exactly, but simpler: no Kafka transport-duplicate concept, so classification is just accepted/superseded/quarantined by latest-`retrieved_at`-per-symbol, and no aggregation step - gold is a direct projection of the accepted rows). Registered `fundamentals` in `src/export/gold_snapshot.py`'s `DATASET_BUSINESS_KEYS`/`DATASET_CUTOFF_COLUMN` and in `src/load/snowflake_snapshot.py`'s `DATASET_STAGING_TABLE`/`DATASET_SERVING_TABLE`/`DATASET_SQL_FILES`/`DATASET_COLUMNS` - both registries were deliberately built generic across datasets in R2/R4, so this needed no redesign, only new entries plus [snowflake/sql/004_staging_fundamentals.sql](../snowflake/sql/004_staging_fundamentals.sql)/[005_serving_fundamentals.sql](../snowflake/sql/005_serving_fundamentals.sql). Added a second job resource (`landed_fundamentals`) to `databricks/databricks.yml` and its script entry to `databricks/pyproject.toml`. Added two new GitHub Actions workflows: [fetch-fundamentals.yaml](../.github/workflows/fetch-fundamentals.yaml) (runs the fetcher; no Databricks CLI/Terraform needed, same simplicity as `export-gold-snapshot.yaml`) and [deploy-databricks-fundamentals-job.yaml](../.github/workflows/deploy-databricks-fundamentals-job.yaml) (mirrors `deploy-databricks-job.yaml`'s fixture/validate/deploy/run/verify pattern, with its own deterministic fixture and idempotent fixed S3 key, learning directly from the ticks job's earlier fixture-accumulation bug rather than repeating it). The existing `export-gold-snapshot.yaml`/`load-snowflake-snapshot.yaml` workflows need no changes at all - both already take `dataset` as a free-text input, so `dataset=fundamentals` just works. <br><br>Added a "Fundamentals Snapshot Contract (R6)" section to `docs/hybrid-migration.md` documenting the new landing format/classification semantics, and sections to `databricks/README.md`/`snowflake/README.md` pointing at it. 40 new tests across `tests/unit/test_landed_fundamentals.py` (pure-Python `normalize_fundamentals_record`, no Spark needed, mirroring `test_landed_ticks.py`'s style exactly), `tests/unit/test_databricks_fundamentals.py` (mocked-Spark job-runner tests, mirroring `test_databricks_ticks.py` line-for-line where the logic is identical - every mirrored test passed on the first run, a good sign the new job runner faithfully follows the proven pattern), and `tests/unit/test_fundamentals_fetcher.py` (mocked yfinance/S3, including the fetch-partial-failure and refuse-empty-batch paths). Full suite: 469 passed (was 402 - some of that growth is R4/R5 tests from earlier in this session), 86.25% coverage; ruff/black/mypy clean; all new YAML/TOML syntax-validated; the new SQL DDL verified to render correctly through the loader's `.format()` substitution (same check used for R4's DDL). **Not yet run**: `terraform fmt/validate/test` and a live apply for the broadened `landing` location (Terraform CLI still unavailable in this environment), a live run of `deploy-databricks-fundamentals-job.yaml`, `fetch-fundamentals.yaml`, and the existing export/load workflows with `dataset=fundamentals` - that's the next concrete action, most likely to surface at least one real bug on first contact with a live workspace, consistent with every other product built this migration |
| 2026-09-26 | First live run of `fetch-fundamentals.yaml` failed: yfinance rate-limited every symbol; added retry/backoff | All 10 symbols got `429 Too Many Requests` from Yahoo Finance's unofficial API within ~250ms of each other - the classic signature of a burst rate limit (fetch_one had zero delay between the 10 back-to-back requests), not necessarily a permanent IP block. `run_fetch` correctly refused to upload an empty batch rather than pretending success (`RuntimeError: All 10 symbols failed to fetch`) - the safety behavior worked exactly as designed, it just had nothing to work with. Fixed in `src/producers/fundamentals_fetcher.py`: `fetch_one` now retries each symbol up to 3 times with backoff (2s/5s/10s) before giving up, and `run_fetch` spaces requests 1.5s apart between symbols (not just retrying after the fact - reducing the chance of tripping the burst limit in the first place). Both delays are injectable (`sleep` parameter) so tests never actually block - added a dedicated regression test (`test_fetch_one_retries_and_recovers_from_a_transient_failure`, a fake Ticker that fails twice then succeeds) and updated the always-fails test to assert the exact backoff sequence, rather than just re-mocking away the new behavior. Full suite: 470 passed (was 469), 86.36% coverage; ruff/black/mypy clean. **Genuinely unresolved**: whether this fixes it depends on whether Yahoo's 429 was a burst-rate-limit (this fix should resolve it) or a broader block on GitHub Actions' shared runner IP ranges specifically (a known community complaint about yfinance from CI environments) - only a live re-run will tell. If 429s persist even with backoff/spacing, the fetcher is a standalone script with no GitHub Actions dependency and can be run locally instead (`python -m src.producers.fundamentals_fetcher --bucket ...`, using the same AWS credentials already in `.env`) from a residential IP, which is far less likely to be rate-limited. **Not yet re-run**: `fetch-fundamentals.yaml` with this fix - that's the next concrete action |

### Required Update After Every Implementation Session

1. Update the date, status table, current gate, and next concrete action.
2. Append a ledger row with changes, relevant file links/revision, checks actually
   run, results, blockers, and any change to prior decisions. Use "uncommitted"
   when appropriate; never invent a commit or imply CI ran because local tests passed.
3. Separate implementation, local/static validation, deployment, and live verification.
   Record non-secret environment/run identifiers and cleanup status for authorized
   cloud work. Never paste credentials, full state, or secret-bearing outputs.
4. Update the owning contract/runbook when behavior changes and link it here. Mark
   superseded assumptions and failed checks explicitly rather than hiding them.
5. Leave the next assistant a small actionable task, its acceptance checks, and
   any authorization/account information still needed. No hidden TODOs in chat only.

## Continuation Prompt

```text
Read docs/implementation-handover.md first, then .github/copilot-instructions.md
and the scoped instructions for the files you touch. Verify the branch/worktree
and current file contents. Continue from the current gate and remaining roadmap,
preserving the local baseline and existing user edits. Do not deploy, start paid
workloads, or delete data without explicit authorization. Distinguish local
checks from real cloud evidence. Update the handover status, validation record,
change ledger, blockers, and next step before finishing the session.
```

## Later Review And Optimization

When the original assistant or another reviewer returns, compare this ledger to
the code and Git changes since `07cb728`. Review actual deployed resource state,
least-privilege grants, version/manifest semantics, failure/replay/correction/delete
handling, no-input cost gates, dashboard freshness, state protection, and full
destruction including residual resources. Reconcile sample results across platforms,
run relevant gates, and assess measured spend/latency before optimizing compute or
adding abstractions. A feature inventory is not evidence of end-to-end correctness.